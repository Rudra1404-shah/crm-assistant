[project]
name = "crm-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rudra1404-shah", email = "shahrudra1404@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "langchain>=1.4.2",
    "langchain-openai>=1.6.4",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
crm-assistant = "crm_assistant:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
